Update Log - 07-04-2026

This document lists everything that was modified during the implementation and fixes for the Xrus web app.

1. Authentication and User Account Features
- Fixed user registration flow and improved username generation.
- Added username uniqueness handling to avoid duplicate usernames.
- Fixed login flow so inactive users are blocked from signing in.
- Corrected the seeded admin password so admin login works with:
  - Email: admin@musicshop.com
  - Password: admin
- Added admin support for creating users from the admin panel.
- Added admin support for updating user information.
- Added admin support for deleting users.
- Added admin support for activating and deactivating users.
- Added admin support for changing user roles.

2. Admin User Management UI
- Added a user management page with Add User, Edit User, Delete User, Toggle Status, and Update Role actions.
- Added role selection using a dropdown in the user table.
- Added a dedicated Update Role button for each user.
- Added a modal form for creating and editing users.

3. Backend User Routes and Controllers
- Added/updated backend endpoints:
  - POST /api/v1/users
  - GET /api/v1/users
  - PUT /api/v1/users/:id
  - DELETE /api/v1/users/:id
  - PUT /api/v1/users/:id/status
  - PUT /api/v1/users/:id/role
- Updated the user controller to support admin CRUD actions and role/status changes.
- Added validation for user creation and updates.

4. Item Management CRUD
- Repaired the item CRUD page so it works correctly.
- Updated the item form to use the correct item fields:
  - name
  - description
  - brand
  - buy_price
  - sell_price
  - stock
  - category_id
  - status
  - image
- Fixed item create, fetch, update, and delete operations.
- Improved the item page search/filter behavior.

5. Homepage Search and Product Browsing
- Added a search box on the homepage.
- Implemented live filtering for products by name.
- Added autocomplete-style suggestions for product search.
- Improved product rendering from the backend API.

6. Frontend Files Modified
- xrus-front/users.html
- xrus-front/js/users.js
- xrus-front/item.html
- xrus-front/js/item.js
- xrus-front/home.html
- xrus-front/js/home.js
- xrus-front/header.html

7. Backend Files Modified
- xrus-back/controllers/user.js
- xrus-back/routes/user.js
- xrus-back/controllers/item.js

8. Verification Performed
- Verified that the backend server runs successfully on port 4000.
- Verified that the admin login endpoint works.
- Verified that the item listing endpoint returns data from the database.
- Verified that the protected admin user routes respond correctly.


- - - - - - -  - - - - - - - - - - - - - - - - - -
7/4/2026 - 10pm

9. Connection troubleshooting using express
- Replace backend index.js port from 3000 to 4000
- app.js express add path to front end folder
- index.js test database connection code
- Checklist.txt to see which ones are working and not

10. Frontend JavaScript structure alignment
- Added the missing frontend JavaScript files expected by the desired project structure:
  - xrus-front/js/login.js
  - xrus-front/js/register.js
  - xrus-front/js/profile.js
  - xrus-front/js/helpers.js
- Kept the new scripts consistent with the existing jQuery and Swal patterns already used in the project.
- Followed the current frontend naming and coding style without introducing extra files.

11. Multiple Image Support + DataTables Integration Update
- Added item_images table (item_id, image_path, FK cascade) while keeping items.image as primary image (no breaking changes to existing queries)
- Updated upload logic: upload.single → upload.array('images', 10) for multi-image support
- Controllers updated to store first image in items.image and all uploads in item_images (backward-compatible)
- getSingleItem now returns full images[] array
- deleteItem now also removes related item_images records
- Frontend switched to DataTables ($('#itable').DataTable) for built-in search, sort, pagination
- Added multi-image upload + preview in UI and removed manual search box
- CRUD actions now refresh table via ajax.reload() without full reload

12. Profile Load + Cart Order bug fix + order front end
- Added orders front end for admin
- Fixed profile bug where user is logged in but user details are not loaded on profile
- fix issue where added items are all present on all carts regardless of user
- fix issue where user cannot checkout their order
- item stock now decreases after succesful user checkout


- - - - - - - - - - - - - - -
7/6/2026 - 8:30 PM

13. Front End Design Improvements
- Adjusted header for all pages where it appeared to float
- Added Missing header for login and register, cart pages
- Added Missing header for admin pages: item, users, orders
- Adjusted Admin dashboard where some results overtake the whole page
- Improved CSS Design from classic bootsrap blue to gray

14. mp4 and mp7 issue fixes
